import TextReveal from "@/components/ui/text-reveal";

export function TextRevealDemo() {
  return (
    <div className="hidden md:flex min-h-64 items-center justify-center rounded-lg bg-white dark:bg-primary-dark">
      <TextReveal text="Will add more stuff as time goes by, so don't" />
    </div>
  );
}
 